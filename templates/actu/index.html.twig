{% extends 'base2.html.twig' %}

{% block title %}Actu index{% endblock %}

{% block body %}
    <h1>Actu index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Contenu</th>
                <th>Ladate</th>
                <th>Slug</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for actu in actus %}
            <tr>
                <td>{{ actu.id }}</td>
                <td>{{ actu.titre }}</td>
                <td>{{ actu.contenu }}</td>
                <td>{{ actu.ladate ? actu.ladate|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ actu.slug }}</td>
                <td>
                   {#  <a href="{{ path('actu_show', {'id': actu.id}) }}">show</a> #}
                    <a href="{{ path('actu_edit', {'id': actu.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">Aucun enrégistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('actu_new') }}">Nouveau</a>

{# 
        <div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Tables</h1>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-heading">
                            DataTables Advanced Tables
                        </div>
                        <!-- /.panel-heading -->
                        <div class="panel-body">
                            <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
                                <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Titre</th>
                                        <th>Contenu</th>
                                        <th>Ladate</th>
                                        <th>Slug</th>
                                        <th>actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                   {% for actu in actus %}
                                        <tr class="gradeU">
                                        <td>{{ actu.id }}</td>
                                        <td>{{ actu.titre }}</td>
                                        <td>{{ actu.contenu }}</td>
                                        <td>{{ actu.ladate ? actu.ladate|date('Y-m-d H:i:s') : '' }}</td>
                                        <td>{{ actu.slug }}</td>
                                        <td>
                                            <a href="{{ path('actu_show', {'id': actu.id}) }}">show</a>
                                            <a href="{{ path('actu_edit', {'id': actu.id}) }}">edit</a>
                                        </td>
                                    </tr>
                                     {% else %}
                                    <tr>
                                        <td colspan="6">Aucun enregistrement trouvé</td>
                                    </tr>
                                   {% endfor %}
                                    
                                </tbody>
                            </table>
                            <!-- /.table-responsive -->

                            
                           
                        </div>
                        <!-- /.panel-body -->
                    </div>
                    <!-- /.panel -->
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
           
          
           
        </div> #}
        <!-- /#page-wrapper -->

{% endblock %}
