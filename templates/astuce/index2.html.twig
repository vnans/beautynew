{% extends 'base.html.twig' %}

{% block title %}Astuce liste{% endblock %}

{% block body %}
 <section id="subscribe">
    <div class="container wow fadeInUp">
      <div class="section-header">
        <h2>. </h2>
         <h2>BEAUTY AU 98025</h2>
        <p>
        Recevez par SMS les conseils, les astuces et secrets de beauté pour un corps de
        rêve.
       </p>
      </div>

      {# <form method="POST" action="#">
        <div class="form-row justify-content-center">
          <div class="col-auto">
            <input type="text" class="form-control" placeholder="Enter your Email">
          </div>
          <div class="col-auto">
            <button type="submit">Subscribe</button>
          </div>
        </div>
      </form> #}

    </div>
  </section>  {#   <h1>les  Astuces beauty</h1> #}
 <!--==========================
    Speakers Section
  ============================-->
  <section id="speakers" class="wow fadeInUp">
    <div class="container">
      <div class="section-header">
        <h2>Astuces de beauté</h2>
        <p>Astuces de beauté </p>
      </div>

      <div class="row">
 {% for astuce in astuces %}
          <div class="col-lg-4 col-md-6">
          <div class="speaker">
            <img src=" {{ asset('img/') }}{{ astuce.image }} " alt="Speaker 6" class="img-fluid">
            <div class="details">
              <h3><a href="{{ path('astuce_show',{'id': astuce.id}) }}">{{ astuce.titre }} </a></h3>
              <p></p>
              <div class="social">
                <a href=""><i class="fa fa-twitter"></i></a>
                <a href=""><i class="fa fa-facebook"></i></a>
                <a href=""><i class="fa fa-google-plus"></i></a>
                <a href=""><i class="fa fa-linkedin"></i></a>
              </div>
            </div>
          </div>
        </div>

{% endfor %}

      </div>
     
    </div>

  </section>

   {#  <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Contenu</th>
                <th>Image</th>
                <th>Ladate</th>
                <th>Slug</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for astuce in astuces %}
            <tr>
                <td>{{ astuce.id }}</td>
                <td>{{ astuce.titre }}</td>
                <td>{{ astuce.contenu }}</td>
                <td>{{ astuce.image }}</td>
                <td>{{ astuce.ladate ? astuce.ladate|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ astuce.slug }}</td>
                <td>
                    <a href="{{ path('astuce_show', {'id': astuce.id}) }}">Afficher</a>
                    <a href="{{ path('astuce_edit', {'id': astuce.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">Aucun enrégistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('astuce_new') }}">Nouveau</a> #}
{% endblock %}
